import SlotMachine from '@/components/SlotMachine';

export default function Home() {
  return (
    <main className="min-h-screen w-full bg-gradient-to-br from-black via-red-950 to-black flex items-center justify-center p-4">
      <SlotMachine />
    </main>
  );
}
